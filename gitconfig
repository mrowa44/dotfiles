[user]
  name  = Justyna Rachowicz
  email = mrowa44@gmail.com
[alias]
  a   = add
  aa  = add --all
  br  = branch
  cm  = commit -v
  co  = checkout
  df  = diff
  dfc = diff --cached
  m   = merge
  pl  = pull
  plr = pull --rebase
  psh = push
  rb  = rebase
  rh  = reset HEAD
  sh  = stash
  st  = status
  cp  = cherry-pick
  lg  = log --graph --color=always --format='%C(auto)%h%d %s %C(black)%C(bold)~%an %Creset%C(black)%cr'
[grep]
  lineNumber = true
[push]
  default = current
[color]
  ui     = auto
  status = auto
  branch = auto
  diff   = auto
[log]
  decorate = true
[help]
  autocorrect = 1
[core]
  excludesfile = ~/.gitignore
[pager]
  diff = diff-so-fancy | less --tabs=1,5 -RFX
  show = diff-so-fancy | less --tabs=1,5 -RFX
